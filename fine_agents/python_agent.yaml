name: Python Agent for Voice-Activated ChatGPT CLI
description: This agent handles the Python backend for voice recognition, text-to-speech, and GPT-3 integration.

environment:
  docker:
    image: python:3.9

dependencies:
  shell:
    - pip install --upgrade pip
    - pip install SpeechRecognition pyttsx3 openai pytest pylint requests

commands:
  build: 
    shell: echo "No build steps required for Python"
  test:
    shell: pytest tests/
  run:
    shell: python main.py
